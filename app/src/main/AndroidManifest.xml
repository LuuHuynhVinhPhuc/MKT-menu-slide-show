<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name="signin.sign_in"
            android:exported="false"
            android:label=""
            android:theme="@style/AppTheme.Dialog"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity
            android:name="slideshow.menuSlider.menuSlider"
            android:exported="false">
        </activity>
        <activity
            android:name="admin_Settings.schedule_foods.galleryGet.upload_Images"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name="slideshow.autoSlider.sliderView"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="choose_options.choose_options"
            android:exported="false" />
        <activity
            android:name="admin_Settings.API.api_get_link"
            android:exported="false" />
        <activity
            android:name="admin_Settings.schedule_foods.showEvent.schedule_Food_items"
            android:exported="false" />
    </application>
</manifest>